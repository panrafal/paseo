/**
 * Find marks are painted through the CSS Custom Highlight API, which only exists on
 * web. See highlight-colors.web.tsx for the component that keeps their colors in
 * step with the theme.
 */
export function FindHighlightColorsSync() {
  return null;
}
