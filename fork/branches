# Branches merged into `main`, this fork's integration branch, in order.
#
# One git ref per line. Blank lines and `#` comments are ignored.
# Use `origin/<name>` for branches you push; a bare name means a local branch.
# Order matters only for conflict resolution — put the branch you want to
# "win" the base last.
#
# fork-base is not listed: it is the branch this file lives on, and the
# sync always merges it first.
#
# When a PR lands upstream, delete its line. The next sync drops it.


# Open PRs against getpaseo/paseo.
origin/add-daemon-loopback-password-bypass
origin/add-paseo-deeplink-hash-prompts

# Fork features not yet sent upstream.
origin/vscode
